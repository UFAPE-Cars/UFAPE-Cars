<p style="color: green"><%= notice %></p>
<%= link_to 'Home', root_path %>

<h1>Veiculos</h1>
<%= link_to 'Buscar Veículos', buscar_veiculos_path %>

<div id="veiculos">
  <% @veiculos.each do |veiculo| %>
    <%= render veiculo %>
    <p>
      <%= link_to "Show this veiculo", veiculo %>
    </p>
  <% end %>
</div>

<%= link_to "New veiculo", new_veiculo_path %>
